#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int* prefix(char* pattern) {
  int m = strlen(pattern);
  int pi[m];

  printf("%i",m);
  return pi;
}


int main() {
  
  prefix("aadsgsdfga");
  return 0;
}
